# ============================================================================
# HAND-AUTHORED. NOT generated by Terraform — this file holds hosts that
# Terraform doesn't (and shouldn't) manage. Loaded alongside the
# Terraform-generated hosts.ini — see ansible.cfg's `inventory` setting.
# Add further non-Terraform-managed hosts here.
# ============================================================================
[pi3]
pi3-01 ansible_host=192.168.68.6 ansible_user=bcochofel

# Pure grouping group (no hosts of its own) spanning both the Terraform-owned
# dns group and the pi3 group above — every host that runs a Pihole
# container. Used to target both instances from playbooks/05-dns.yml and
# from group_vars/pihole.yml.
[pihole:children]
dns
pi3
