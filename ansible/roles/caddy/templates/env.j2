# Managed by Ansible. Read by Caddy at container start via env_file — never
# templated directly into the Caddyfile (Caddyfile uses {$CLOUDFLARE_API_TOKEN}
# / {$LETSENCRYPT_EMAIL} placeholder syntax instead).
CLOUDFLARE_API_TOKEN={{ cloudflare_api_token }}
LETSENCRYPT_EMAIL={{ letsencrypt_email }}
